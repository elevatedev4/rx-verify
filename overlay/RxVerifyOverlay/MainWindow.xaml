<Window x:Class="RxVerifyOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:RxVerifyOverlay.Models"
        Title="Rx Verify"
        Width="420" Height="560"
        MinWidth="340" MinHeight="300"
        Topmost="True"
        WindowStartupLocation="Manual"
        Left="20" Top="20"
        ResizeMode="CanResizeWithGrip"
        Background="#FF1E1E1E">
    <!--
        Always-on-top (Topmost="True" — a hard requirement: the
        pharmacist needs this visible over PioneerRx at all times), and
        freely movable/resizable like any normal window (drag the title
        bar, drag the resize grip) so Will can park it wherever fits his
        monitor layout. Left/Top are just an initial position, not a
        pin — WPF remembers wherever the user drags it for the rest of
        that run; persisting position across launches is a nice-to-have,
        not done in v0 (see README "Deferred").
    -->
    <Window.Resources>
        <SolidColorBrush x:Key="GreenBrush" Color="#FF2E7D32" />
        <SolidColorBrush x:Key="YellowBrush" Color="#FFF9A825" />
        <SolidColorBrush x:Key="RedBrush" Color="#FFC62828" />
        <SolidColorBrush x:Key="NeutralBrush" Color="#FF3A3A3A" />
        <!-- GrayBrush: category box/header when there's NO DATA at all
             (HasData=false), distinct from GreenBrush so "no data" never
             reads as "everything matches" (see CategoryViewModel.HasData). -->
        <SolidColorBrush x:Key="GrayBrush" Color="#FF555555" />

        <!-- Muted/dark variants of the same hues for the WHOLE category
             box background (not just the header strip) -- full-strength
             GreenBrush/YellowBrush/RedBrush across an entire card would
             wash out the white row text, so the box uses a darker tint of
             the same color while the header keeps the bold version. -->
        <SolidColorBrush x:Key="BoxGreenBrush" Color="#FF1B3A1E" />
        <SolidColorBrush x:Key="BoxYellowBrush" Color="#FF4A3B12" />
        <SolidColorBrush x:Key="BoxRedBrush" Color="#FF4A1F1F" />
        <SolidColorBrush x:Key="BoxGrayBrush" Color="#FF2A2A2A" />

        <Style x:Key="PillTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Title / summary counts -->
            <RowDefinition Height="Auto" /> <!-- Status message -->
            <RowDefinition Height="Auto" /> <!-- Toolbar buttons -->
            <RowDefinition Height="*" />    <!-- Verdict list (fixed order, never resorted) -->
            <RowDefinition Height="Auto" /> <!-- Settings (collapsible-ish, just always shown small in v0) -->
        </Grid.RowDefinitions>

        <!-- Row 0: title + green/yellow/red summary pills -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Rx Verify" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Border Background="{StaticResource GreenBrush}" CornerRadius="4" Padding="8,3" Margin="2,0">
                    <TextBlock Style="{StaticResource PillTextStyle}" Text="{Binding GreenCount, StringFormat=✓ {0}}" />
                </Border>
                <Border Background="{StaticResource YellowBrush}" CornerRadius="4" Padding="8,3" Margin="2,0">
                    <TextBlock Style="{StaticResource PillTextStyle}" Text="{Binding YellowCount, StringFormat=? {0}}" />
                </Border>
                <Border Background="{StaticResource RedBrush}" CornerRadius="4" Padding="8,3" Margin="2,0">
                    <TextBlock Style="{StaticResource PillTextStyle}" Text="{Binding RedCount, StringFormat=✗ {0}}" />
                </Border>
            </StackPanel>
        </Grid>

        <!-- Row 1: status line -->
        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Foreground="#FFBBBBBB" FontSize="11"
                   TextWrapping="Wrap" Margin="0,0,0,8" />

        <!-- Row 2: toolbar -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Refresh" Padding="10,4" Margin="0,0,6,0" Click="OnRefreshClick" />
            <Button Content="Dump UIA Tree..." Padding="10,4" Margin="0,0,6,0" Click="OnDumpTreeClick" />
            <CheckBox x:Name="AutoRefreshCheckBox" Content="Auto (5s)" Foreground="White" VerticalAlignment="Center"
                      Checked="OnAutoRefreshToggled" Unchecked="OnAutoRefreshToggled" />
        </StackPanel>

        <!--
            Row 3: compact grouped table — 3 categories (Patient /
            Prescriber / Rx, see Models/EngineModels.cs FieldCategories),
            each with a rolled-up status header (worst-status-wins: any
            Red field -> Red header, else any Yellow -> Yellow, else
            Green) and its field rows below in small print, FIXED
            PHARMACIST FIELD ORDER within the category (never re-sorted —
            hard requirement: rows must mirror the data-entry screen's
            order, never jump around by severity). Binds directly to
            OverlayViewModel.Categories (built/rolled-up every refresh in
            OverlayViewModel.RefreshAsync). Reason code + explanation are
            NOT always-visible text anymore (that's what made the old
            per-field-card layout tall) — they're a hover tooltip on the
            row instead (VerdictRowViewModel.TooltipText), so the whole
            table stays small enough to see all 10 fields at once for a
            normal Rx without scrolling.
        -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Column header labels for the compact table below -->
            <Grid Grid.Row="0" Margin="4,0,4,3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="62" />
                    <ColumnDefinition Width="*" MinWidth="70" />
                    <ColumnDefinition Width="*" MinWidth="70" />
                    <ColumnDefinition Width="14" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Field" Foreground="#FF888888" FontSize="9" FontWeight="Bold" />
                <TextBlock Grid.Column="1" Text="Source" Foreground="#FF888888" FontSize="9" FontWeight="Bold" Margin="4,0,0,0" />
                <TextBlock Grid.Column="2" Text="Entered" Foreground="#FF888888" FontSize="9" FontWeight="Bold" Margin="4,0,0,0" />
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Categories}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!--
                                Whole box colored by the category's rolled-up
                                status (Will's live-test feedback: previously
                                only the header strip was colored, so a
                                Red/Yellow field buried in the row list below
                                could be missed at a glance). NO DATA (HasData
                                =false) renders GRAY, never green -- green
                                means "data present AND matches", not "nothing
                                to complain about yet".
                            -->
                            <Border BorderBrush="#FF3A3A3A" BorderThickness="1" CornerRadius="4" Margin="0,0,0,6">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource BoxGreenBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="{x:Static models:VerdictStatus.Yellow}">
                                                <Setter Property="Background" Value="{StaticResource BoxYellowBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="{x:Static models:VerdictStatus.Red}">
                                                <Setter Property="Background" Value="{StaticResource BoxRedBrush}" />
                                            </DataTrigger>
                                            <!-- HasData=false wins over any Status value (checked last so it overrides). -->
                                            <DataTrigger Binding="{Binding HasData}" Value="False">
                                                <Setter Property="Background" Value="{StaticResource BoxGrayBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <!-- Category header: name + rolled-up status glyph -->
                                    <Border CornerRadius="3,3,0,0" Padding="6,3">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource GreenBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static models:VerdictStatus.Yellow}">
                                                        <Setter Property="Background" Value="{StaticResource YellowBrush}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static models:VerdictStatus.Red}">
                                                        <Setter Property="Background" Value="{StaticResource RedBrush}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding HasData}" Value="False">
                                                        <Setter Property="Background" Value="{StaticResource GrayBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="12" VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="1" Text="{Binding Glyph}" Foreground="White" FontWeight="Bold" FontSize="12" VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>

                                    <!-- Field rows: Field | Source | Entered | status dot -->
                                    <ItemsControl ItemsSource="{Binding Rows}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="6,3" ToolTipService.ToolTip="{Binding TooltipText}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="56" />
                                                        <ColumnDefinition Width="*" MinWidth="70" />
                                                        <ColumnDefinition Width="*" MinWidth="70" />
                                                        <ColumnDefinition Width="14" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding DisplayName}" Foreground="#FFBBBBBB" FontSize="10" TextWrapping="Wrap" VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="1" Text="{Binding SourceValue}" Foreground="White" FontSize="10" TextWrapping="Wrap" Margin="4,0" VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="2" Text="{Binding EnteredValue}" Foreground="White" FontSize="10" TextWrapping="Wrap" Margin="4,0" VerticalAlignment="Center" />
                                                    <Ellipse Grid.Column="3" Width="10" Height="10" VerticalAlignment="Center">
                                                        <Ellipse.Style>
                                                            <Style TargetType="Ellipse">
                                                                <Setter Property="Fill" Value="{StaticResource GreenBrush}" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static models:VerdictStatus.Yellow}">
                                                                        <Setter Property="Fill" Value="{StaticResource YellowBrush}" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static models:VerdictStatus.Red}">
                                                                        <Setter Property="Fill" Value="{StaticResource RedBrush}" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Ellipse.Style>
                                                    </Ellipse>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- Row 4: engine path settings, always visible in v0 (small footprint, no PHI) -->
        <Expander Grid.Row="4" Header="Engine settings" Foreground="White" Margin="0,8,0,0">
            <StackPanel Margin="0,6,0,0">
                <TextBlock Text="rx-verify dist/cli.js path:" Foreground="#FFBBBBBB" FontSize="11" />
                <Grid Margin="0,2,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="CliPathTextBox" Grid.Column="0" Padding="4" />
                    <Button Grid.Column="1" Content="Browse..." Margin="4,0,0,0" Padding="8,4" Click="OnBrowseCliPathClick" />
                </Grid>

                <TextBlock Text="Node executable (usually just 'node'):" Foreground="#FFBBBBBB" FontSize="11" />
                <TextBox x:Name="NodeExeTextBox" Padding="4" Margin="0,2,0,6" />

                <Button Content="Save settings" Padding="8,4" HorizontalAlignment="Left" Click="OnSaveSettingsClick" />
            </StackPanel>
        </Expander>
    </Grid>
</Window>
